[tool:pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --tb=short

[coverage:run]
source = src
omit = tests/*

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    raise NotImplementedError
